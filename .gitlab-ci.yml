image: maven:3.6.3-jdk-11

stages:
  - test
  - deploy

cache:
  paths:
    - .m2/repository

test-maven:
  stage: test
  tags:
    - ILT
  script:
    - "mvn verify"

deploy-maven:
  stage: deploy
  tags:
    - ILT
  only:
    - master
  script:
    - 'mvn deploy -s ci_settings.xml'
